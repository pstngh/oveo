# Generate a new identity with:
# age-keygen -o /etc/oveo/backup.agekey
# Then put the output of `age-keygen -y /etc/oveo/backup.agekey` here.
AGE_RECIPIENT=age1replace_with_the_public_recipient
AGE_IDENTITY_FILE=/etc/oveo/backup.agekey

